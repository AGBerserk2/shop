// The delivery module powers live driver tracking. It needs no global
// config or hooks — its migration, pages, API endpoints and GraphQL
// types are auto-discovered by EverShop.
export default () => {};
